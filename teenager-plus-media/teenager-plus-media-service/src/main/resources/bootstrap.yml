spring:
  application:
    name: media-service
  cloud:
    nacos:
      server-addr: 121.37.109.0:8848
      discovery:
        namespace: ${spring.profiles.active}
        group: teenager-plus-project
      config:
        namespace: ${spring.profiles.active}
        group: teenager-plus-project
        file-extension: yaml
        refresh-enabled: true

#profiles默认为dev
  profiles:
    active: dev




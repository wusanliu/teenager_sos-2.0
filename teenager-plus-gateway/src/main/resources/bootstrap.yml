#微服务配置
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 121.37.109.0:8848
      discovery:
        namespace: dev
        group: teenager-plus-project
      config:
        namespace: dev
        group: teenager-plus-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: logging-${spring.profiles.active}.yaml
            group: teenager-plus-common
            refresh: true


  profiles:
    active: dev